import React from 'react';

const ContactSection = ({
  animateTransition,
  isRightToLeft,
  isDarkMode,
}: {
  animateTransition: boolean;
  isRightToLeft: boolean;
  isDarkMode: boolean;
}) => {
  return (
    <div>
      <h1>Contact</h1>
    </div>
  );
};
