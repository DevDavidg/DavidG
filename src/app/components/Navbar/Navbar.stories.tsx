
import React, {Fragment} from 'react';
import Navbar from './Navbar';

export default {title: 'Component|Navbar'};

export const NavbarExample = () => (
    <Fragment>
        <Navbar />
    </Fragment>
);
